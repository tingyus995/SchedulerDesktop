import javax.swing.*;
import java.awt.*;

public class ScheduleView extends JPanel {
    ScheduleView(){
        super();
        setBackground(Color.GREEN);
        JLabel label = new JLabel("ScheduleView", SwingConstants.CENTER);
        add(label);

    }
}
